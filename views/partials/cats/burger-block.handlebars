<li>
	{{burger_name}}

	<button class="devour-burger" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}Delete!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Devour!</button>
</li>
